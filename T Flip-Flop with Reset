// t_flip_flop_reset.v

module t_flip_flop (
    input T,
    input CLK,
    input RESET,
    output reg Q
);

    always @(posedge CLK) begin

        if (RESET)
            Q <= 1'b0;

        else if (T)
            Q <= ~Q;

        else
            Q <= Q;

    end

endmodule
