// t_flip_flop.v

module t_flip_flop (
    input T,
    input CLK,
    input RESET,
    output reg Q
);

    always @(posedge CLK or posedge RESET) begin

        if (RESET)
            Q <= 1'b0;

        else if (T)
            Q <= ~Q;

    end

endmodule
