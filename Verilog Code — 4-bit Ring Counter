// ring_counter_4bit.v

module ring_counter_4bit (
    input CLK,
    input RESET,
    output reg [3:0] Q
);

    always @(posedge CLK) begin

        if (RESET)
            Q <= 4'b1000;

        else
            Q <= {Q[2:0], Q[3]};

    end

endmodule
