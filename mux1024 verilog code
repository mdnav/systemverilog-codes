// verilog code
module mux1024 (
    input  [1023:0] I,
    input  [9:0] S,
    output reg Y
);

integer i;

always @(*) begin
    Y = 1'b0;

    for (i = 0; i < 1024; i = i + 1) begin
        if (S == i)
            Y = I[i];
    end
end

endmodule
