// sipo_shift_register_4bit.v

module sipo_shift_register_4bit (
    input CLK,
    input RESET,
    input Serial_In,
    output reg [3:0] Q
);

    always @(posedge CLK) begin

        if (RESET)
            Q <= 4'b0000;

        else
            Q <= {Q[2:0], Serial_In};

    end

endmodule
